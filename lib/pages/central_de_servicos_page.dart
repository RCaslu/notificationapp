import 'package:flutter/material.dart';

class CentralDeServicosPage extends StatelessWidget {
  const CentralDeServicosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Central de Serviços')),
      body: Center(child: const Text('Welcome to the Central de Serviços page')),
    );
  }
}
